# Mission: 파이썬 멘탈 모델 뇌 이식 (Python Mental Model Transplant)

## Why
파이썬 코드를 마주했을 때 표면적 문법 암기에 의존하지 않고, 메모리 힙(Heap) 공간에서 객체들이 어떻게 할당되고 참조(포인터) 화살표들이 어떻게 연결되는지 CPython 시스템 레벨에서 0.1초 만에 머릿속에 시각화하여 복잡한 버그와 동작 원리를 꿰뚫어 본다.

## Success looks like
- x = 10이나  = [1, 2]를 볼 때 변수를 상자가 아닌 '이름표(Label)'로, 데이터를 '힙에 뜬 독립 객체(PyObject)'로 메모리 화살표를 즉시 스케치할 수 있다.
- my_dog.bark() 실행 시 인스턴스와 클래스 사이에서 데이터(상태)와 로직(함수)이 어떻게 분리되어 있고, 점(.) 연산자가 왜 새로운 <bound method> 임시 래퍼를 힙에 생성하는지 인과 관계를 설명할 수 있다.
- 메서드의 self 매개변수가 가리키는 화살표의 실제 방향(함수 → 인스턴스)을 헷갈리지 않고 정확하게 지목할 수 있다.
- 불변(Immutable) 객체와 가변(Mutable) 객체의 메모리 재할당/인플레이스 수정 차이를 객체 그래프 관점에서 예측하여 참조 공유 버그를 원천 차단한다.
- C 언어 추상 기계의 메모리 모델(스택 상의 바이트 저장 영역)과 파이썬 객체(헤더와 메타데이터를 지닌 힙 노드)의 본질적 차이를 분별할 수 있다.

## Constraints
- 표면적 문법 설명 배제, C/CPython 메모리 레이아웃 및 객체 그래프 인과 관계 중심.
- 가독성을 저해하는 불필요한 메타데이터나 어수선한 서식(span 태그 등) 배제, 극도로 정갈한 Tufte 스타일 타이포그래피 유지.
- 퀴즈 설계 시 선택지 글자 수와 형태를 균형 있게 구성하여 직관적 인지 편향 차단.

## Out of scope
- Django, FastAPI 등 웹 프레임워크 사용법.
- 알고리즘 코딩 테스트 풀이 기교.
- 비CPython 구현체(Jython, IronPython 등)의 특이 동작.
